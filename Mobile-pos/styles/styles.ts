import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 20,
  },

  title: {
    fontSize: 22,
    marginBottom: 20,
  },

  input: {
    borderWidth: 1,
    padding: 10,
    marginBottom: 10,
    borderRadius: 6,
  },

  buttonGap: {
    height: 10,
  },

  card: {
    padding: 15,
    borderWidth: 1,
    borderRadius: 8,
    marginVertical: 8,
  },

  statusText: {
    marginTop: 5,
  },
});
